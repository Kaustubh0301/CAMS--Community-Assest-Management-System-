# CAMS Documentation

Index of project documentation. Each subdirectory holds one category. These files
are the durable record — keep them in sync with the code.

| Directory | Purpose | Status |
|-----------|---------|--------|
| `requirements/` | Functional and non-functional requirements | empty — to be written |
| `architecture/` | System structure, module boundaries, key flows | empty — to be written |
| `database/` | Schema design, migrations, data dictionary | empty — to be written |
| `api/` | API reference and conventions | empty — to be written |
| `testing/` | Test strategy, coverage targets, test data | empty — to be written |
| `decisions/` | Architecture Decision Records (ADRs) — approved decisions | empty — to be written |

Nothing in these directories yet describes real technical specifications. Do not
treat placeholder text as a specification. See `../PLAN.md` for what is proposed
versus approved.

/**
 * Asset Category module (MVP).
 *
 * Admin-configurable category list per local body, seeded with defaults
 * (D23). No code change is required to add a category.
 *
 * See docs/architecture/SYSTEM_ARCHITECTURE.md §5.
 * No business logic is implemented yet — Phase 1 skeleton only.
 */
package com.cams.backend.assetcategory;

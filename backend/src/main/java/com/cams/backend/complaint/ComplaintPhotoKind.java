package com.cams.backend.complaint;

/** Maps the PostgreSQL enum type {@code complaint_photo_kind} (D11). */
public enum ComplaintPhotoKind {
    CITIZEN_REPORT,
    WORKER_BEFORE,
    WORKER_COMPLETION
}

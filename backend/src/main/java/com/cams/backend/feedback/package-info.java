/**
 * Feedback module (MVP).
 *
 * One citizen rating (+ optional one-time note) per CLOSED complaint (D2,
 * D17); exposes rating for analytics and for the owning worker only (D28).
 * Rating scale remains OQ-37 (open).
 *
 * See docs/architecture/SYSTEM_ARCHITECTURE.md §5.
 * No business logic is implemented yet — Phase 1 skeleton only.
 */
package com.cams.backend.feedback;
